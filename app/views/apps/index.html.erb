<div class="panel panel-default">
  <div class="panel-heading">
    <div class="row">
      <div class="col-sm-10">
        <h5>App列表</h5>
      </div>
      <div class="col-sm-2">
        <%= link_to "新建App", new_app_path, class: "btn btn-success pull-right" %>
      </div>
    </div>
  </div>
  <div class="panel-body">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>ID</th>
          <th>Icon</th>
          <th>App名</th>
          <th>App Secret Key</th>
          <th>Store Url</th>
          <th>App类型</th>
          <th>App平台</th>
          <th>version</th>
          <th>(Bundle id/package name)</th>
          <th>操作</th>
        </tr>
      </thead>

      <tbody>
        <% @apps.each do |app| %>
          <tr>
            <td width="6%"><%= app.id %></td>
            <td width="10%"><%= render_app_icon(app) %></td>
            <td width="8%"><%= app.name %></td>
            <td width="20%"><%= app.secret_key %></td>
            <td width="20%"><%= app.store_url %></td>
            <td width="5%"><%= app.app_type %></td>
            <td width="5%"><%= app.platform %></td>
            <td width="5%"><%= app.version %></td>
            <td width="10%"><%= app.app_id %></td>
            <td width="10%">
              <%= link_to "编辑", edit_app_path(app), class: "btn btn-xs btn-primary" %>
              <%= link_to "删除", app_path(app), method: :delete, data: { confirm: "您确定吗？" }, class: "btn btn-xs btn-danger" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

</div>

