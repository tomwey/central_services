defaults: &defaults
  app_name: "<%= fetch(:application) %>"
  domain: "<%= fetch(:server_name) %>:<%= fetch(:port) %>"
  admin_emails:
    - "admin@admin.com"
  email_sender: 'noreply@shuiguoshe.com'
  upload_url: "http://<%= fetch(:server_name) %>:<%= fetch(:port) %>"

<%= fetch(:rails_env) %>:
  <<: *defaults